// react uses a system called synthetic event it wraps browser event to work the same way across all browser it nbehaves like regular java script event but work consistently
import React from "react";
export function SyntheticEventThree(){
    function handleInput(e){
        
        
    }
}